<div class="flex justify-center">
    <div class="w-2/3">
        <div class="">
            <div class="text-teal-700 font-bold text-2xl my-4">New Review</div>
        </div>
        <div class="flex justify-center">
            <%= render 'form', review: @review, repo_id: @review.repository_id, commits: @commits, repository: @review.get_repository %>
        </div>
    </div>
</div>