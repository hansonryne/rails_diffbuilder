<div class="hidden py-4 px-4 border-l border-b border-r" data-target="tabs.panel">
  <p>
  <strong>Reviews:</strong>
  <table class="my-4">
    <thead>
      <tr>
        <th>Start date</th>
        <th>Owner</th>
        <th>Status</th>
        <th colspan="1"></th>
      </tr>
    </thead>
    <tbody>
      <% repository.reviews.each do |r| %>
        <tr>
          <td><%= r.start_date %></td>
          <td><%= r.owner %></td>
          <td><%= r.get_status %></td>
          <td><%= link_to 'View', r %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  </p>

  <div>
    <%= link_to 'Create Review of this Repo', new_review_path(repo_id: repository.id), class: "selection-target border border-teal-700 hover:bg-teal-700 hover:text-white text-teal-700 font-bold py-1 px-2 my-4 rounded" %>
  </div>
</div>
